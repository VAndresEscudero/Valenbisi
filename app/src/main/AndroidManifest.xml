<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.uv.app.victor.valenbisi">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="es.uv.app.victor.valenbisi.ListaParadas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="es.uv.app.victor.valenbisi.InformacionParada"/>

        <activity android:name="es.uv.app.victor.valenbisi.ListaPartes"
            android:windowSoftInputMode="adjustNothing"/>
    </application>

</manifest>